<%- include('./partials/head')%>
<link href="/css/details.css" rel="stylesheet">
</head>

<body>
    <header>
        <%- include('./partials/nav')%>
    </header>
    <main>
        <div id="product-detail-container">
            <div id="product-img-table">
                <img src="<%= product.image_url %>" alt="<%= product.name %>">
                <p id="product-info-header">Product Information</p>
                <table>
                    <tbody>
                        <tr>
                            <th scope="row">Model</th>
                            <td><%= product.model %></td>
                        </tr>
                        <tr>
                            <th scope="row">Dimensions</th>
                            <td><%= product.dimensions %></td>
                        </tr>
                        <tr>
                            <th scope="row">Material</th>
                            <td><%= product.material %></td>
                        </tr>
                        <tr>
                            <th scope="row">Color</th>
                            <td><%= product.color %></td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div id="product-description">
                <h5><%= product.name%></h5>
                <p><%= product.description %></p>
                <h6 id="price">Price: <%=product.price%></h6>
                <form method="POST" action="/cart/add" id="add-to-cart-form">
                    <input type="hidden" name="product_id" value="<%= product.product_id %>">
                    <input type="number" name="quantity" value="1" min="1" required id="quantity-input">
                    <button type="submit" id="cart-btn">Add to Cart</button>
                  </form>                  
            </div>
        </div>
    </main>
    <%- include('./partials/footer')%>
</body>

